spring:
  application:
    name: discovery
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://localhost:8888
      username: secret
      password: secret
      fail-fast: true

