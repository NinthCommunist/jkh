server:
  port: 8888

spring:
  application:
    name: jkh-config-server
  profiles:
    active: native


  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:./configs/
  security:
    user:
      name: secret
      password: secret